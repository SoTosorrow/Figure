1、定时器：
    真实时间定时器:
        永久移除定时器
    帧时间定时器

2、Renderer：
    是否要将绘制部分改为static静态？工厂模式生产？
    或者绘制时只是统一的调用static的Render
    绘制部分参数的字典化
    分层式与图元式
3、碰撞与范围检测模块
5、局部坐标变化Transform模块

4、整理代码

模块的优先级改造：

10、资源池/消息队列 代替 app指针。设定一个资源池被所有模块共有，
    比如dispatcher就把mouse的对象引用交给资源池，然后其他模块就可以拿取（后期考虑调度处理
    

func2 = func1 代码移交，此时bind是否同时移交了上下文？bind的用处、箭头函数和bind的关系，箭头函数好像有this相关注意点？

中间件系统：
    中间件可以对所有或者某些模块做拦截器类的作用

图元管理模块：
    包含一些了图元，实现了render的都可以被管理（/
    在模块的update中遍历所有图元的render
玩家模块
游戏规则模块
webGL模块


注意（待再测
阴影的绘制效率很低（未测
圆的绘制效率低几倍
虚线的绘制效率低几倍，
    不加虚线1w个Rect会在20帧徘徊，加了只有几帧
    不加虚线1k个Rect能有60帧，加了只有不到20帧
image的性能
    